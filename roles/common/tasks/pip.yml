---
- name: Install pip
  become: true
  when: ansible_os_family ==  "Debian"
  apt:
    name: python3-pip
    update_cache: yes
    state: latest

- name: Update pip
  pip:
    name: pip
    state: latest
